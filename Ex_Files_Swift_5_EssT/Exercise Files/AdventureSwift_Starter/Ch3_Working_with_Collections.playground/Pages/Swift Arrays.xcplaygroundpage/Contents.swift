/*:
 # Swift Arrays
 ---
 
 ## Topic Essentials
 Swift arrays are **ordered lists**, meaning that the position of each element is determined by the order it was added. Arrays work off of indexes, which can be used to access and modify the values of individual items.
 
 ### Objectives
 + Create arrays with different syntax
 + Add initial values
 + Use the `count` and `isEmpty` methods
 + Access and update array values using subscripts
 
 [Next Topic](@next)
 
 */
var levelDifficulty: [String] = []
let anotherSyntax : Array<String> = Array<String>()

var testDiff = [1,2,3,4]

//anotherSyntax.append(contentsOf: "Swift")

print(levelDifficulty)
print(anotherSyntax)
print(testDiff[testDiff.count - 1])



















//// Creating arrays
//var levelDifficulty: [String] = []
//var extraSyntax1: Array<String> = Array<String>()
//
//levelDifficulty = ["C", "C++", "Swift"]
//
//// Count and isEmpty
//extraSyntax1.isEmpty
//print(levelDifficulty.count)
//
//levelDifficulty[0] = "Python"
//levelDifficulty[2] = "Java"
////levelDifficult
//
//print(levelDifficulty[levelDifficulty.count - 1])
//// Accessing array values
//
//
//levelDifficulty.append("PHP")
//
//levelDifficulty += ["N!", "N2", "N3"]
//
//levelDifficulty.insert("Superman", at: 1)
//
//let popedItem = levelDifficulty.popLast()
//
//levelDifficulty.reverse()
//
//print(popedItem!)
//print(levelDifficulty)
//
//var newArr: [[Int]] = [
//    [1,2,3,8],
//    [4,5,6],
//    [7,8,4]
//]
//
//print(newArr.count)
//print(newArr.capacity)
//
//print(newArr[0].count)
